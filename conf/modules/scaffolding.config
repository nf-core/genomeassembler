process {
    withName: '.*SCAFFOLD:.*RAGTAG_SCAFFOLD' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/ragtag/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_ragtag" }
        ext.args   = [
                "-C",
                "-u",
                "-r",
                "-w"
            ].join(" ").trim()
    }
    withName: '.*:RUN_RAGTAG:BGZIP.*' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/ragtag/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_ragtag" }
    }

    withName: LINKS {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/links/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_links" }
        ext.args = ["-t 40,200", "-d 500,2000,5000"].join(" ").trim()
    }
    withName: '.*:RUN_LINKS:BGZIP.*' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/links/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_links" }
    }
    withName: '.*:RUN_LONGSTITCH:BGZIP.*' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/longstitch/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_longstitch" }
    }
    withName: LONGSTITCH {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/longstitch/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_longstitch" }
    }
    withName: BWAMEM2_INDEX {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/bwamem2/index" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_bwamem_index" }
    }
    withName: BWAMEM2_MEM {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/bwamem2/mem" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_bwamem" }
        ext.args   = { "-5SP"}
    }
    withName: MINIMAP2_HIC {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/minimap/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_minimap_hic" }
        ext.args   = { "--no-pairing" }
    }
    withName: MARKDUP {
        ext.args = "-Djava.io.tmpdir=./tmp-picard-mardkdup --CREATE_INDEX"
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/markdup/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_hic_dedup" }
    }
    withName: ADD_RG {
        ext.args = {[ "--RGID 1", "--RGLB ${meta.id}","--RGPM UNKNOWN", "--RGPL ILLUMINA","--RGPU 0", "--RGSM ${meta.id}"].join(" ").trim()}
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/add_replace_rg/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_add_rg" }
    }
    withName: YAHS {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/yahs/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_hic" }
    }
    withName: '.*:HIC:BGZIP.*' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/yahs/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_hic" }
    }
    withName: '.*HIC:SAMTOOLS_FAIDX.*' {
        publishDir = [
            path: { "${params.outdir}/${meta.id}/scaffold/hic/yahs/index/" },
            mode: params.publish_dir_mode
        ]
        ext.prefix = { "${meta.id}_hic_dedup" }
    }
}
