/*
    Use this config to:
        assemble HIFI reads with hifiasm
        assemble ONT reads with flye
        scaffold the flye assembly onto the hifiasm assembly
*/

params {
    hifi = true
    ont = true
    assembler = "flye_on_hifiasm"
}
