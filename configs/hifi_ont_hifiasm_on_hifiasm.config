/*
    Use this config to:
        assemble HIFI reads with hifiasm
        assemble ONT reads with hifiasm --ont
        scaffold the ONT assembly onto the HiFi assembly
*/

params {
    hifi = true
    ont = true
    assembler = "hifiasm_on_hifiasm"
}
